@model IEnumerable<Perpustakaan.Models.Kategori>

@{
    ViewData["Title"] = "Index";
}

<h1>Kategori</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Nama Kategori")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaKategori)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKategori"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdKategori"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdKategori"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
