@model IEnumerable<Perpustakaan.Models.Buku>

@{
    ViewData["Title"] = "Index";
}

<h1>Buku</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Judul Buku")
            </th>
            <th>
                @Html.DisplayName("Harga")
            </th>
            <th>
                @Html.DisplayName("Deskripsi")
            </th>
           
            <th>
                @Html.DisplayName("Kategori")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JudulBuku)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Harga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Deskripsi)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.IdKategoriNavigation.NamaKategori)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBuku"><i class="fa fa-marker fa-lg"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdBuku"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdBuku"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
